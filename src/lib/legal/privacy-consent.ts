/**
 * Константы согласия на обработку персональных данных.
 *
 * Отдельный модуль от `privacy-policy.ts` намеренно: текст политики подставляет
 * реквизиты из `company.ts`, который читает серверные env-переменные (COMPANY_NAME
 * и прочие, без префикса NEXT_PUBLIC). Формы — клиентские компоненты, и импорт
 * версии политики из общего файла затянул бы чтение этих переменных в клиентский
 * бандл. Здесь только чистые константы, безопасные для клиента.
 */

/**
 * Редакция политики. Пишется в запись лида вместе с фактом согласия: через год
 * надо уметь ответить, на какую именно редакцию согласился конкретный человек.
 *
 * Менять при каждом изменении текста политики — вместе с датой в шапке страницы.
 */
export const PRIVACY_POLICY_VERSION = '2026-08-21';

/** Канонический адрес политики. С /privacy стоит 301 в next.config.mjs. */
export const PRIVACY_POLICY_PATH = '/legal/privacy';
